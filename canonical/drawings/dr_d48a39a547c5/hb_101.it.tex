\begin{tikzpicture}[
    scale=1.0,
    >=stealth,
    every node/.style={font=\small}
]

% ============================================================
% Elliptische Satellitenbahn
% ============================================================

% Halbachsen
\def\a{5}
\def\b{3}
\def\c{4}

% Terra im Brennpunkt
\coordinate (Earth) at (-\c,0);

% Ellipse
\draw[thick] (0,0) ellipse (\a cm and \b cm);

% ============================================================
% Terra
% ============================================================

\filldraw[
    fill=blue!55,
    draw=blue!20!black,
    thick
]
(Earth) circle (0.42cm);

\node[below left=4pt] at (Earth)
    {\textbf{Terra}};

% ============================================================
% Zwei Satellitenpositionen
% ============================================================

% Position 1: weit von der Terra entfernt
\coordinate (S1) at (4.0,2.0);

% Position 2: näher bei der Terra
\coordinate (S2) at (-1.0,-2.83);

% Satellit an Position 1
\filldraw[
    fill=gray!50,
    draw=black,
    thick
]
(S1) circle (0.14cm);

% Satellit an Position 2
\filldraw[
    fill=gray!50,
    draw=black,
    thick
]
(S2) circle (0.14cm);

% ============================================================
% Fahrstrahlen
% ============================================================

\draw[thick] (Earth) -- (S1);
\draw[thick] (Earth) -- (S2);

% ============================================================
% Überstrichene Flächen
%
% Die Flächen werden schematisch als gleich grosse
% Sektoren dargestellt.
% ============================================================

% Fläche A1
\fill[
    gray!25,
    opacity=0.8
]
(Earth) --
(4.0,2.0)
arc[
    start angle=28,
    end angle=14,
    x radius=8.25cm,
    y radius=8.25cm
] -- cycle;

% Fläche A2
\fill[
    gray!25,
    opacity=0.8
]
(Earth) --
(-1.0,-2.83)
arc[
    start angle=-125,
    end angle=-105,
    x radius=3.47cm,
    y radius=3.47cm
] -- cycle;

% ============================================================
% Geschwindigkeiten
% ============================================================

% Geschwindigkeitsvektor nahe Terra
\draw[very thick,->]
    (S2) -- ++(-1.1,0.25)
    node[below left] {\textbf{più veloce}};

% Geschwindigkeitsvektor weit entfernt
\draw[very thick,->]
    (S1) -- ++(-0.45,0.35)
    node[above right] {\textbf{più lento}};

% ============================================================
% Beschriftung der beiden Flächen
% ============================================================

\node at (1.1,1.05)
    {$A_1$};

\node at (-1.9,-1.1)
    {$A_2$};

\node[align=center] at (1.0,-3.9)
    {$A_1=A_2$\\
     stesso tempo};

% ============================================================
% Grosse Halbachse
% ============================================================

\draw[<->,thin]
    (-5.46,-1.99) -- (4.54,-1.99)
    node[midway,below] {semiasse maggiore $a$};

% ============================================================
% Keplersche Gesetze
% ============================================================

\node[
    align=left,
    anchor=west
] at (-5.2,4.15)
{
\textbf{1. Prima legge di Keplero}\\
Il satellite si muove su un’ellisse.\\
La Terra si trova in un fuoco.
};

\node[
    align=left,
    anchor=west
] at (-5.2,-4.35)
{
\textbf{2. Seconda legge di Keplero}\\
In tempi uguali vengono percorse aree uguali.
};

\node[
    align=left,
    anchor=west
] at (1.4,-4.35)
{
\textbf{3. Terza legge di Keplero}\\
$\displaystyle \frac{T^2}{a^3}=\mathrm{konstant}$
};

\end{tikzpicture}